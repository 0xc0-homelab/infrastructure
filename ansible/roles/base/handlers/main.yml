# Validate first: a broken sshd config would lock everyone out.
- name: Validate sshd config
  ansible.builtin.command: /usr/sbin/sshd -t
  changed_when: false
  listen: Restart ssh

- name: Restart sshd
  ansible.builtin.systemd_service:
    name: ssh
    state: restarted
  listen: Restart ssh
