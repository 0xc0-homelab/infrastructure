# Handlers run in check mode too: on a first dry run the unit does not exist.
- name: Reapply homelab forwarding
  ansible.builtin.systemd_service:
    name: homelab-forwarding
    state: restarted
    daemon_reload: true
  when: not (ansible_check_mode and node_forwarding_unit.changed)
