# Tooling for the infrastructure repo.
# Declares everything it needs: CI clones this repo on its own.
[tools]
opentofu = "1.12.6"
packer   = "1.16.1"
ansible  = "14.4.0"
sops     = "3.13.3"
age      = "1.3.2"
jq       = "1.8.2"

"pipx:ansible-lint" = "26.9.0"

[env]
# Same path on macOS and Linux. Without it sops looks in a macOS-only location.
SOPS_AGE_KEY_FILE = "{{env.HOME}}/.config/sops/age/keys.txt"
