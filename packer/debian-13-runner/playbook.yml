# Run by Packer on the build VM, cloned from debian-13-base: the runner's
# install entry point. Nothing here is per VM or secret.
- name: Bake the runner template
  hosts: all
  become: true
  tasks:
    - name: Install the runner
      ansible.builtin.include_role:
        name: github_runner
        tasks_from: install
